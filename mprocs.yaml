procs:
  trunk: "cargo leptos watch"
  tailwind: 
    shell: "npx tailwindcss -i ./style/tailwind.css -o ./style/generated.css --watch"
    stop: "SIGKILL"
  fmt: 
    shell: "cargo fmt"
    autostart: false
  tests:
    shell: "cargo test"
    autostart: false